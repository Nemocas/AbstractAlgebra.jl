language: julia
sudo: false
addons:
  apt:
    packages:
      - texlive
os:
  - linux
  - osx
julia:
  - 0.4
  - release
  - nightly
notifications:
  - email: false
#script:
  - julia -e 'Pkg.clone(pwd()); Pkg.test("Nemo")'
